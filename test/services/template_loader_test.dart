import 'package:test/test.dart';

void main() {
  group('TemplateLoader', () {
    test(
      'loads valid yaml',
      () async {},
    );

    test(
      'throws when file does not exist',
      () async {},
    );

    test(
      'throws on malformed yaml',
      () async {},
    );

    test(
      'loads empty package list',
      () async {},
    );
  });
}
